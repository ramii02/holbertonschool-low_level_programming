#include "holberton.h"
#include <stdio.h>
/**
* main - check the code for Holberton School students.
* @c: checks for uppercase character
* Return: Always 0.
*/
int _isupper(int c);

{
if ('% d, c > 65 && % d, c > 90')
return (1)
else
return (0)
}
